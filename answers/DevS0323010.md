# DevS0323010

<!-- Public fork: use only your GitHub username. Do not add a student ID,
Discord name, legal name, email address, or other private identifier. -->

## Environment

OS: Debian 13 GNU/Linux

Editor / IDE: tilde, codeblocks, pycharm, vscodium, zed

Shell: bash

Languages I have used: C, C++, Python

## Things I have done before

- [V] SSH into another machine
- [ ] Resolve a Git merge conflict
- [ ] Build a Docker image
- [ ] Read a stack trace
- [V] Compile software from source
- [ ] Use a debugger
- [V] Use Linux as a primary development environment
- [ ] `sudo rm -rf /` a server ???!
- [ ] <others ...>

## Something I built

Digitized push-pull box, RMG-V2 simple music generator

## Something I want to understand better

How to use CMake and MakeFile for C/C++ development

## Mission 01 — Linux

### Task A — find the file

Path: missions/01-linux/files/.config/nested/.deep/.treasure

Command I used: python3 -c "from pathlib import Path; cur = Path.cwd(); contents = [cur]; op = lambda x: open(x, 'r'); rd = lambda x: [x.read(), x.close()][0]; [print([exit() if len(contents) == 0 else None, contents.extend([x for x in contents[0].iterdir()]) if contents[0].is_dir() else (str(contents[0]) if 'THE_PENGUIN_WAS_HERE' in rd(op(contents[0])).strip().split() else None), contents.pop(0)][1]) for i in range(10000)]"

### Task B — count the errors

Count: 8

Command I used: python3 -c "f = open('server.log', 'r'); v = f.read().split('\n'); print(sum(['ERROR' in i for i in v])); f.close();"

## Mission 03 — SSH

SSH token: FLAG{fe322c8dc745}

Command I used: 
chmod 600 knock\ knock
ssh -i knock\ knock flag@217.142.229.247

## Mission 04 — Debug

What was wrong:

What I changed:

## Mission 05 — Docker

What was wrong:

What I changed:

## Mission 06 — Improve something

What I changed:

Why:
